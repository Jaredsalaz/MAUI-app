<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MAUI.Pages.WelcomePage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="White">

    <Grid Padding="30" RowSpacing="20" VerticalOptions="CenterAndExpand">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Image Source="welcome_logo.png" HeightRequest="100" Aspect="AspectFit" HorizontalOptions="Center" Grid.Row="0" />

        <Label Text="Citas médicas" 
               FontFamily="League Spartan"
               FontSize="48"
               FontAttributes="None"
               TextColor="#2260FF"
               HorizontalOptions="Center" 
               Grid.Row="1" />

        <Label Text="Doctores" 
               FontFamily="League Spartan"
               FontSize="24"
               FontAttributes="None"
               TextColor="#2260FF"
               HorizontalOptions="Center" 
               Grid.Row="2" />

        <Label Text="Cada diagnóstico, cada tratamiento y cada sonrisa que brindas son un regalo para todos"
               FontSize="14" 
               TextColor="Gray" 
               HorizontalOptions="Center" 
               HorizontalTextAlignment="Center" 
               Margin="0,20,0,0" 
               Grid.Row="3" />

        <Button Text="Iniciar Sesión" 
                BackgroundColor="#2260FF" 
                TextColor="White" 
                FontFamily="League Spartan"
                FontSize="24"
                FontAttributes="Bold"
                CornerRadius="20" 
                HeightRequest="50" 
                WidthRequest="200"
                HorizontalOptions="Center" 
                Clicked="OnLoginClicked" 
                Grid.Row="4" />

        <Button Text="Registrarse" 
                BackgroundColor="#E0E0E0" 
                TextColor="#2260FF" 
                FontFamily="League Spartan"
                FontSize="24"
                FontAttributes="Bold"
                CornerRadius="20" 
                HeightRequest="50" 
                WidthRequest="200"
                HorizontalOptions="Center" 
                Clicked="OnRegisterClicked" 
                Grid.Row="5" />
    </Grid>

</ContentPage>