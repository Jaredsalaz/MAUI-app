<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:MAUI.Pages"
       x:Class="MAUI.AppShell">

    <ShellItem>
        <ShellContent Title="Welcome" ContentTemplate="{DataTemplate local:WelcomePage}" />
        <ShellContent Title="Login" ContentTemplate="{DataTemplate local:LoginPage}" />
        <ShellContent Title="Register" ContentTemplate="{DataTemplate local:RegisterPage}" />
        <ShellContent Title="Appointments" ContentTemplate="{DataTemplate local:AppointmentPage}" />
        <ShellContent Title="Set Password" ContentTemplate="{DataTemplate local:SetPasswordPage}" />
        <ShellContent Title="OTP" ContentTemplate="{DataTemplate local:OTPPage}" />
    </ShellItem>

</Shell>